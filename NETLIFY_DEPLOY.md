# Legacy Netlify Guide

This project now uses **Vercel** for the frontend instead of Netlify.

Use `VERCEL_DEPLOY.md` for the current deployment steps.

Current stack:

- Frontend: Vercel
- Backend: Render
- Auth/database: Supabase

Current backend API:

```text
https://dailytrade-backend.onrender.com
```

Health check:

```text
https://dailytrade-backend.onrender.com/health
```
